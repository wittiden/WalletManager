[tool.ruff]
target-version = 'py312'

line-length = 88

exclude = [
    '.venv',
    '__pycache__',
    '.git',
]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]

ignore = ['E501']

[tool.ruff.format]
quote-style = 'single'